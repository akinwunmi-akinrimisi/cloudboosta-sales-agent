---
phase: 08-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - execution/dashboard/src/App.jsx
  - execution/dashboard/src/components/Login.jsx
  - execution/dashboard/src/components/LiveView.jsx
  - execution/dashboard/src/components/ActiveCallCard.jsx
  - execution/dashboard/src/components/StatCard.jsx
  - execution/dashboard/src/components/RecentCallsTable.jsx
autonomous: true
requirements: [DASH-01, DASH-05]

must_haves:
  truths:
    - "Unauthenticated user sees a login screen with token input field"
    - "After entering valid token, user sees the dashboard with 3 tabs"
    - "Invalid token shows error message and returns to login"
    - "Token persists in localStorage across page refreshes"
    - "Live View shows active call card with green pulse when a call is live"
    - "Live View shows 4 stat cards (Total Calls, Connected, Committed, Conversion Rate)"
    - "Live View shows recent calls table with outcome badges and expandable rows"
    - "Live View data refreshes every 5 seconds via polling"
    - "Empty state shown when no calls exist yet"
  artifacts:
    - path: "execution/dashboard/src/components/Login.jsx"
      provides: "Token input login screen"
      min_lines: 25
    - path: "execution/dashboard/src/App.jsx"
      provides: "Auth-gated tab shell"
      contains: "getToken"
    - path: "execution/dashboard/src/components/LiveView.jsx"
      provides: "Live View tab with polling"
      contains: "useInterval"
    - path: "execution/dashboard/src/components/ActiveCallCard.jsx"
      provides: "Hero card with pulse indicator"
      contains: "animate-pulse"
    - path: "execution/dashboard/src/components/StatCard.jsx"
      provides: "Reusable stat card"
    - path: "execution/dashboard/src/components/RecentCallsTable.jsx"
      provides: "Expandable recent calls table"
      contains: "OutcomeBadge"
  key_links:
    - from: "execution/dashboard/src/App.jsx"
      to: "execution/dashboard/src/api.js"
      via: "getToken import"
      pattern: "import.*getToken.*from.*api"
    - from: "execution/dashboard/src/components/LiveView.jsx"
      to: "/api/dashboard/live"
      via: "apiFetch('/live')"
      pattern: "apiFetch.*live"
    - from: "execution/dashboard/src/components/LiveView.jsx"
      to: "execution/dashboard/src/hooks/useInterval.js"
      via: "useInterval(fetchLive, 5000)"
      pattern: "useInterval.*5000"
---

<objective>
Build the authentication gate (login screen + token validation) and the Live View tab with all its sub-components: active call card with pulse indicator, 4 stat cards, and expandable recent calls table with outcome badges and recording player.

Purpose: Authentication is the entry point for the entire dashboard. Live View is the primary monitoring tab -- the operator's real-time view of Sarah's calling activity. Both must work before any other tab is meaningful.

Output: Login.jsx, auth-gated App.jsx, fully functional LiveView with ActiveCallCard, StatCard, RecentCallsTable.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard/08-CONTEXT.md
@.planning/phases/08-dashboard/08-RESEARCH.md
@.planning/phases/08-dashboard/08-01-SUMMARY.md

@execution/dashboard/src/App.jsx
@execution/dashboard/src/api.js
@execution/dashboard/src/hooks/useInterval.js
@execution/dashboard/src/constants.js
@execution/dashboard/src/components/EmptyState.jsx
@execution/dashboard/src/components/OutcomeBadge.jsx

<interfaces>
<!-- From Plan 01 (api.js) -->
export function getToken(): string | null
export function setToken(token: string): void
export function clearToken(): void
export async function apiFetch(path: string): Promise<any>

<!-- From Plan 01 (useInterval.js) -->
export function useInterval(callback: () => void, delay: number | null): void

<!-- From Plan 01 (constants.js) -->
export const POLL_LIVE: 5000
export const OUTCOME_COLORS: { [outcome: string]: { bg, text, dot } }
export function maskPhone(phone: string): string
export function formatDuration(seconds: number): string
export function formatTime(isoString: string): string

<!-- From Plan 01 (EmptyState.jsx) -->
export default function EmptyState({ icon?, title, message }): JSX.Element

<!-- From Plan 01 (OutcomeBadge.jsx) -->
export default function OutcomeBadge({ outcome }): JSX.Element

<!-- Backend /api/dashboard/live response shape -->
{
  active_call: { id, name, phone, status, programme_recommended, last_strategy_used, last_call_at } | null,
  recent_calls: [{ id, lead_id, retell_call_id, started_at, ended_at, duration_seconds, outcome, closing_strategy_used, detected_persona, summary, recording_url, lead_name, lead_phone }],
  today_stats: { total_calls, connected, committed, conversion_rate }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login screen and auth gate in App.jsx</name>
  <files>execution/dashboard/src/components/Login.jsx, execution/dashboard/src/App.jsx</files>
  <action>
1. **Create src/components/Login.jsx:**
   - State: token (string), error (string), loading (boolean)
   - Render a centered card on a neutral background (min-h-screen flex items-center justify-center)
   - Card contains: "Sarah Dashboard" heading, brief description "Cloudboosta Sales Agent Monitor", token input field (type="password" for security), "Sign In" button
   - On submit: call apiFetch("/live") with the entered token set temporarily via setToken. If 200, the token is valid -- setToken permanently and call onLogin callback. If error/401, clearToken(), show "Invalid token" error message, clear the input.
   - Input styling: Tailwind border, rounded, focus:ring, full width. Button: bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded. Error: text-red-500 text-sm.
   - Prop: onLogin (callback when authenticated)

2. **Update src/App.jsx:**
   - Import { getToken, clearToken } from "../api" (or "./api" depending on file location)
   - Import Login from "./components/Login"
   - Add state: const [authenticated, setAuthenticated] = useState(!!getToken())
   - Auth gate: if (!authenticated) return <Login onLogin={() => setAuthenticated(true)} />
   - Add logout button in header: small "Logout" text button that calls clearToken() + setAuthenticated(false)
   - Keep existing tab navigation and conditional rendering unchanged
   - Keep existing TABS array, header, nav, main structure
  </action>
  <verify>
    <automated>cd execution/dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Login screen renders when no token. Valid token shows dashboard. Invalid token shows error. Logout button clears token. Token persists in localStorage across refreshes.</done>
</task>

<task type="auto">
  <name>Task 2: Live View tab with active call card, stat cards, and recent calls table</name>
  <files>execution/dashboard/src/components/LiveView.jsx, execution/dashboard/src/components/ActiveCallCard.jsx, execution/dashboard/src/components/StatCard.jsx, execution/dashboard/src/components/RecentCallsTable.jsx</files>
  <action>
1. **Create src/components/ActiveCallCard.jsx:**
   - Props: call (object or null)
   - When call is null: Render grey card with "No Active Call" message, muted text "Waiting for next dial check...", grey status indicator
   - When call exists: Render rich card with:
     - Green pulsing dot indicator (Tailwind animate-pulse on a bg-green-500 rounded-full div)
     - Lead name (bold, large)
     - Masked phone number (use maskPhone from constants)
     - Status badge: "Calling" (amber) or "In Call" (green) based on call.status
     - Programme recommended (if available)
     - Current strategy (call.last_strategy_used, if available)
     - Duration timer: Use local useState + useEffect with setInterval(1s) to count up from call.last_call_at to now. Display as mm:ss via formatDuration.
   - Card styling: White/dark bg, rounded-lg, border, shadow-sm. When live: border-green-500/50 with subtle green glow (shadow-green-500/10).

2. **Create src/components/StatCard.jsx:**
   - Props: label (string), value (string|number), subtitle (optional string), color (optional: "blue"|"green"|"yellow"|"purple", default "blue")
   - Render a compact card: label at top (text-sm text-gray-500), large value centered (text-3xl font-bold), optional subtitle below
   - Color affects the value text color: blue=text-blue-600, green=text-green-600, yellow=text-yellow-600, purple=text-purple-600
   - Card styling: bg-white dark:bg-gray-800, rounded-lg, border, p-4

3. **Create src/components/RecentCallsTable.jsx:**
   - Props: calls (array)
   - If calls is empty: render EmptyState with title "No calls yet today" and message "Recent calls will appear here once the auto-dialer starts."
   - Otherwise: render a table with columns: Time, Lead, Duration, Outcome
   - Each row: formatTime(call.started_at), call.lead_name, formatDuration(call.duration_seconds), OutcomeBadge(call.outcome)
   - Click row to expand: toggle expandedId state. Expanded section shows:
     - Summary text (call.summary or "No summary available")
     - Recording player: HTML5 <audio controls src={call.recording_url} /> (only if recording_url exists)
     - Strategy used, persona detected (small text)
   - Table styling: Tailwind table-auto w-full, striped rows (even:bg-gray-50 dark:even:bg-gray-800/50), hover:bg-gray-100

4. **Rewrite src/components/LiveView.jsx** (replace stub entirely):
   - Import apiFetch, useInterval, POLL_LIVE, ActiveCallCard, StatCard, RecentCallsTable, EmptyState
   - State: data (default { active_call: null, recent_calls: [], today_stats: { total_calls: 0, connected: 0, committed: 0, conversion_rate: 0 } }), error (null)
   - fetchLive: async function wrapped in useCallback, calls apiFetch("/live"), updates data state, clears error. On catch: set error message.
   - Initial fetch: useEffect(() => { fetchLive() }, [fetchLive])
   - Polling: useInterval(fetchLive, POLL_LIVE)
   - Layout (top to bottom):
     - ActiveCallCard with call={data.active_call}
     - 4 StatCards in a grid (grid-cols-2 lg:grid-cols-4 gap-4):
       - Total Calls / data.today_stats.total_calls / color blue
       - Connected / data.today_stats.connected / color green
       - Committed / data.today_stats.committed / color yellow
       - Conversion Rate / data.today_stats.conversion_rate + "%" / color purple
     - RecentCallsTable with calls={data.recent_calls}
   - If error: show small red banner at top with error text and retry button
  </action>
  <verify>
    <automated>cd execution/dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Live View tab renders ActiveCallCard (with pulse when live, grey when idle), 4 stat cards, and expandable recent calls table. Data polls every 5 seconds. Empty states display correctly when no data exists.</done>
</task>

</tasks>

<verification>
1. `cd execution/dashboard && npm run build` passes with zero errors
2. Login.jsx renders centered card with token input
3. App.jsx gates all content behind authentication check
4. LiveView.jsx imports and uses useInterval with POLL_LIVE (5000ms)
5. ActiveCallCard shows pulsing green dot when call.status is "in_call"
6. StatCard renders 4 metrics in a responsive grid
7. RecentCallsTable shows expandable rows with OutcomeBadge and audio player
</verification>

<success_criteria>
- Unauthenticated users see login screen, authenticated users see dashboard
- Live View shows active call card, 4 stat cards, recent calls table
- Polling fetches /api/dashboard/live every 5 seconds
- Empty states display friendly messages when no data exists
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard/08-02-SUMMARY.md`
</output>
