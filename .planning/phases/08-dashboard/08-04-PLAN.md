---
phase: 08-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - execution/dashboard/src/components/StrategyAnalytics.jsx
autonomous: true
requirements: [DASH-04]

must_haves:
  truths:
    - "Strategy Analytics tab shows a horizontal bar chart of conversion rates by closing strategy"
    - "Totals table below the chart shows Strategy, Calls, Connected, Committed, Rate columns"
    - "Empty state with friendly message displayed when no strategy data exists"
    - "Strategy data refreshes every 30 seconds"
    - "Chart renders correctly with Recharts 3 (layout='vertical', XAxis type='number', YAxis type='category')"
  artifacts:
    - path: "execution/dashboard/src/components/StrategyAnalytics.jsx"
      provides: "Strategy analytics tab with chart and table"
      contains: "BarChart"
      min_lines: 60
  key_links:
    - from: "execution/dashboard/src/components/StrategyAnalytics.jsx"
      to: "/api/dashboard/strategy"
      via: "apiFetch('/strategy')"
      pattern: "apiFetch.*strategy"
    - from: "execution/dashboard/src/components/StrategyAnalytics.jsx"
      to: "recharts"
      via: "BarChart import"
      pattern: "import.*BarChart.*from.*recharts"
---

<objective>
Build the Strategy Analytics tab with a horizontal bar chart showing conversion rates per closing strategy (using Recharts 3) and a totals table showing detailed breakdowns, with friendly empty states before any call data exists.

Purpose: Strategy Analytics is the continuous improvement engine -- it tells the operator which closing strategies work best so they can optimize Sarah's approach over time. This is the final data visualization tab completing the dashboard.

Output: Fully implemented StrategyAnalytics.jsx with Recharts horizontal bar chart and totals table.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard/08-CONTEXT.md
@.planning/phases/08-dashboard/08-RESEARCH.md
@.planning/phases/08-dashboard/08-01-SUMMARY.md

@execution/dashboard/src/api.js
@execution/dashboard/src/hooks/useInterval.js
@execution/dashboard/src/constants.js
@execution/dashboard/src/components/EmptyState.jsx

<interfaces>
<!-- From Plan 01 (api.js) -->
export async function apiFetch(path: string): Promise<any>

<!-- From Plan 01 (useInterval.js) -->
export function useInterval(callback: () => void, delay: number | null): void

<!-- From Plan 01 (constants.js) -->
export const POLL_STRATEGY: 30000

<!-- Backend /api/dashboard/strategy response -->
{
  strategies: [
    {
      strategy: "Doctor Frame",
      total_calls: 12,
      committed_count: 5,
      conversion_rate: 41.7,
      personas_seen: 3
    },
    ...
  ]
}

<!-- Recharts 3 horizontal bar chart pattern -->
import { BarChart, Bar, XAxis, YAxis, Tooltip, CartesianGrid, ResponsiveContainer } from "recharts";
<BarChart layout="vertical" data={data}>
  <XAxis type="number" domain={[0, 100]} unit="%" />
  <YAxis type="category" dataKey="strategy" width={140} />
  <Bar dataKey="conversion_rate" fill="#3b82f6" radius={[0, 4, 4, 0]} />
</BarChart>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strategy Analytics tab with horizontal bar chart and totals table</name>
  <files>execution/dashboard/src/components/StrategyAnalytics.jsx</files>
  <action>
Rewrite src/components/StrategyAnalytics.jsx (replace the stub entirely):

1. **Imports:**
   - { useState, useEffect, useCallback } from "react"
   - { BarChart, Bar, XAxis, YAxis, Tooltip, CartesianGrid, ResponsiveContainer, Cell } from "recharts"
   - { apiFetch } from "../api"
   - { useInterval } from "../hooks/useInterval"
   - { POLL_STRATEGY } from "../constants"
   - EmptyState from "./EmptyState"

2. **State and data fetching:**
   - strategies: array, default []
   - error: null
   - fetchStrategy: async useCallback, calls apiFetch("/strategy"), sets strategies from response.strategies
   - Initial fetch: useEffect + fetchStrategy
   - Polling: useInterval(fetchStrategy, POLL_STRATEGY)

3. **Empty state check:**
   - If strategies.length === 0: render EmptyState with:
     - title: "No call data yet"
     - message: "Analytics will appear after Sarah's first calls. Start the auto-dialer to begin collecting data."
   - Return early before rendering chart/table

4. **Horizontal bar chart (Recharts 3):**
   - Wrap in a card container (bg-white dark:bg-gray-800, rounded-lg, border, p-6)
   - Heading: "Conversion Rate by Strategy" (text-lg font-medium mb-4)
   - ResponsiveContainer width="100%" height={Math.max(200, strategies.length * 50)} (dynamic height based on bar count)
   - BarChart layout="vertical" data={strategies} margin={{ top: 5, right: 30, left: 0, bottom: 5 }}
   - CartesianGrid strokeDasharray="3 3" horizontal={false}
   - XAxis type="number" domain={[0, 100]} unit="%" tick={{ fontSize: 12 }}
   - YAxis type="category" dataKey="strategy" width={160} tick={{ fontSize: 12 }}
   - Tooltip: formatter={(value) => value + "%"} with custom content styling
   - Bar dataKey="conversion_rate" fill="#3b82f6" radius={[0, 4, 4, 0]} barSize={24}
   - Add Cell components to color bars by performance: green for rate >= 40, blue for >= 20, gray for < 20 (conditional coloring adds visual meaning)

5. **Totals table:**
   - Below chart in a separate card container (same styling)
   - Heading: "Strategy Performance Summary"
   - HTML table with columns: Strategy, Total Calls, Committed, Conversion Rate, Personas Seen
   - Map strategies array to rows
   - Strategy: bold text
   - Total Calls: right-aligned number
   - Committed: committed_count, right-aligned
   - Conversion Rate: conversion_rate + "%", right-aligned, color-coded text (green >=40, blue >=20, gray <20)
   - Personas Seen: personas_seen count
   - Footer row: totals across all strategies (sum total_calls, sum committed_count, weighted average conversion rate)
   - Table styling: w-full, text-sm, header with bg-gray-50 dark:bg-gray-800/50, alternating row colors

6. **Error state:**
   - Small error banner at top if error is set, with retry button

7. **Overall layout:**
   - space-y-6 container for chart card + table card
  </action>
  <verify>
    <automated>cd execution/dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Strategy Analytics tab renders horizontal bar chart with conversion rates per strategy (Recharts 3), totals table with per-strategy and aggregate stats, and friendly empty state when no data. Polls every 30 seconds. npm run build passes.</done>
</task>

</tasks>

<verification>
1. `cd execution/dashboard && npm run build` passes with zero errors
2. StrategyAnalytics.jsx imports from recharts (BarChart, Bar, XAxis, YAxis)
3. Chart uses layout="vertical" for horizontal bars
4. XAxis type="number" with domain [0,100] and "%" unit
5. YAxis type="category" with dataKey="strategy"
6. Totals table renders all strategy_performance columns
7. Empty state renders when strategies array is empty
8. Data polls /api/dashboard/strategy every 30 seconds
</verification>

<success_criteria>
- Horizontal bar chart displays conversion rates for each closing strategy
- Bars are color-coded by performance level (green/blue/gray)
- Totals table shows per-strategy and aggregate metrics
- Empty state displays friendly message when no strategy data exists
- Data auto-refreshes every 30 seconds
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard/08-04-SUMMARY.md`
</output>
