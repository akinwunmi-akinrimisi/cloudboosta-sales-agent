---
phase: 08-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - execution/backend/main.py
  - execution/dashboard/package.json
  - execution/dashboard/vite.config.js
  - execution/dashboard/src/api.js
  - execution/dashboard/src/hooks/useInterval.js
  - execution/dashboard/src/constants.js
  - execution/dashboard/src/components/EmptyState.jsx
  - execution/dashboard/src/components/OutcomeBadge.jsx
autonomous: true
requirements: [BACK-04, DASH-06]

must_haves:
  truths:
    - "GET /api/dashboard/live returns active_call, recent_calls, and today_stats with real Supabase data"
    - "GET /api/dashboard/pipeline returns leads array with summary fields for kanban"
    - "GET /api/dashboard/strategy returns strategies array from strategy_performance view"
    - "GET /api/dashboard/lead/{id} returns full lead details and call history"
    - "All 4 endpoints reject requests without valid bearer token (401)"
    - "npm run build succeeds with React 19, Vite 6, Recharts 3, Tailwind 3.4"
    - "Shared api.js, useInterval hook, constants, EmptyState, and OutcomeBadge are importable"
  artifacts:
    - path: "execution/backend/main.py"
      provides: "4 real dashboard API endpoints"
      contains: "supabase.table"
    - path: "execution/dashboard/package.json"
      provides: "Upgraded dependencies"
      contains: "react.*19"
    - path: "execution/dashboard/src/api.js"
      provides: "Authenticated fetch wrapper"
      exports: ["apiFetch", "getToken", "setToken", "clearToken"]
    - path: "execution/dashboard/src/hooks/useInterval.js"
      provides: "Declarative polling hook"
      exports: ["useInterval"]
    - path: "execution/dashboard/src/constants.js"
      provides: "Kanban columns, outcome colors, intervals"
      exports: ["KANBAN_COLUMNS", "OUTCOME_COLORS"]
    - path: "execution/dashboard/src/components/EmptyState.jsx"
      provides: "Reusable empty state component"
    - path: "execution/dashboard/src/components/OutcomeBadge.jsx"
      provides: "Color-coded outcome badge"
  key_links:
    - from: "execution/dashboard/src/api.js"
      to: "/api/dashboard/*"
      via: "fetch with bearer token"
      pattern: "fetch.*api/dashboard"
    - from: "execution/backend/main.py"
      to: "supabase views"
      via: "supabase.table() queries"
      pattern: "supabase\\.table\\(.(todays_calls|strategy_performance|leads|call_logs)"
---

<objective>
Implement the 4 backend dashboard API endpoints with real Supabase queries, upgrade the React project dependencies to locked versions (React 19, Vite 6, Recharts 3, Tailwind 3.4), and create the shared frontend infrastructure (auth fetch wrapper, polling hook, constants, reusable components) that all 3 tabs depend on.

Purpose: This plan creates the data layer (backend) and shared utilities (frontend) that Plans 02-04 build upon. Without real API endpoints and shared infrastructure, the tab implementations would duplicate code and have no data source.

Output: Working backend API, upgraded React project, importable shared modules.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard/08-CONTEXT.md
@.planning/phases/08-dashboard/08-RESEARCH.md

@execution/backend/main.py
@execution/backend/schema/004_views.sql
@execution/backend/schema/001_tables.sql
@execution/dashboard/package.json
@execution/dashboard/vite.config.js
@execution/dashboard/src/App.jsx

<interfaces>
<!-- SQL view schemas (deployed in Phase 1, ready to query) -->

todays_calls view columns:
  id, lead_id, retell_call_id, started_at, ended_at, duration_seconds,
  outcome, closing_strategy_used, detected_persona, summary, recording_url,
  lead_name, lead_phone

strategy_performance view columns:
  strategy, total_calls, committed_count, conversion_rate, personas_seen

pipeline_snapshot view columns:
  status, count, oldest, latest

leads table key columns for pipeline:
  id, name, phone, status, updated_at, retry_count, programme_recommended,
  last_call_at, outcome, priority, next_retry_at

call_logs table key columns for lead detail:
  id, lead_id, retell_call_id, started_at, ended_at, duration_seconds,
  outcome, closing_strategy_used, detected_persona, transcript, recording_url,
  summary, from_number, to_number, disconnection_reason

Existing auth in main.py:
  verify_bearer_token(credentials) -> str  (Depends injected, checks DASHBOARD_SECRET_KEY)
  limiter.limit("60/minute") on all dashboard endpoints
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 4 dashboard API endpoints in main.py</name>
  <files>execution/backend/main.py</files>
  <action>
Replace the 3 stub endpoints (lines 625-651) with real Supabase queries, and add a new lead detail endpoint. Specific changes:

1. **GET /api/dashboard/live** -- Replace stub (lines 625-631):
   - Query leads table for status IN ('calling', 'in_call'), limit 1, select id/name/phone/status/programme_recommended/last_strategy_used/last_call_at. This is the active_call.
   - Query todays_calls view, select *, order by started_at desc, limit 10. These are recent_calls.
   - Query todays_calls view, select outcome only, no limit. Compute totals in Python: total = len(rows), connected = count where outcome is not None, committed = count where outcome == 'committed', conversion_rate = round(committed/connected*100, 1) if connected > 0 else 0. Return as today_stats dict with keys total_calls, connected, committed, conversion_rate.
   - Return JSON: { active_call, recent_calls, today_stats }

2. **GET /api/dashboard/pipeline** -- Replace stub (lines 633-642):
   - Query leads table, select id/name/phone/status/updated_at/retry_count/programme_recommended/last_call_at/outcome/priority, order by updated_at desc.
   - Return JSON: { leads: [...] }

3. **GET /api/dashboard/strategy** -- Replace stub (lines 644-651):
   - Query strategy_performance view, select *.
   - Return JSON: { strategies: [...] }

4. **GET /api/dashboard/lead/{lead_id}** -- NEW endpoint after the strategy endpoint:
   - Add path parameter lead_id: str.
   - Protected by verify_bearer_token (same as others).
   - Rate limited 60/minute (same as others).
   - Query leads table, select *, eq id=lead_id, .single().execute().
   - If no data, raise HTTPException 404 "Lead not found".
   - Query call_logs table, select *, eq lead_id=lead_id, order started_at desc.
   - Return JSON: { lead: {...}, calls: [...] }

5. **Fix call_summary column mismatch** in the call_analyzed handler (around line 542):
   - Change `"call_summary": call_summary` to `"summary": call_summary` in the update dict.
   - The table column is named `summary` (001_tables.sql line 92), not `call_summary`.
   - Keep the local variable named `call_summary` (from call_analysis), just fix the dict key.

Also add these missing columns to the call_logs upsert in call_ended handler (around line 407): add `"sentiment"` and `"programme_recommended"` fields are NOT needed here -- they come from call_analyzed and log_call_outcome respectively. No changes to call_ended upsert needed.

Keep all existing decorators (@limiter.limit, Depends(verify_bearer_token)), keep all existing imports.
  </action>
  <verify>
    <automated>cd execution/backend && python -c "from main import app; routes = [r.path for r in app.routes]; assert '/api/dashboard/live' in routes; assert '/api/dashboard/pipeline' in routes; assert '/api/dashboard/strategy' in routes; assert '/api/dashboard/lead/{lead_id}' in routes; print('All 4 dashboard endpoints registered')"</automated>
  </verify>
  <done>4 dashboard API endpoints exist with real Supabase queries. call_summary column name fixed to summary. All endpoints protected by bearer token auth.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade dashboard dependencies and create shared frontend infrastructure</name>
  <files>execution/dashboard/package.json, execution/dashboard/vite.config.js, execution/dashboard/src/api.js, execution/dashboard/src/hooks/useInterval.js, execution/dashboard/src/constants.js, execution/dashboard/src/components/EmptyState.jsx, execution/dashboard/src/components/OutcomeBadge.jsx</files>
  <action>
1. **Update package.json** dependencies to locked versions:
   - react: "^19.0.0" (was ^18.3.1)
   - react-dom: "^19.0.0" (was ^18.3.1)
   - recharts: "^3.0.0" (was ^2.12.0)
   - Keep @supabase/supabase-js: "^2.45.0" (unchanged)
   - devDependencies:
     - vite: "^6.2.0" (was ^5.4.0)
     - @vitejs/plugin-react: "^4.3.4" (was ^4.3.0)
     - tailwindcss: "^3.4.4" (unchanged)
     - postcss: "^8.4.38" (unchanged)
     - autoprefixer: "^10.4.19" (unchanged)

2. **Fix vite.config.js** -- Change server.port from 3000 to 5173 to match DASHBOARD_ORIGIN default. Keep the /api proxy to localhost:8000. Add /retell proxy to localhost:8000 as well (in case dashboard needs to trigger calls).

3. **Create src/api.js** -- Authenticated fetch wrapper:
   - getToken(): return localStorage.getItem("dashboard_token")
   - setToken(token): localStorage.setItem("dashboard_token", token)
   - clearToken(): localStorage.removeItem("dashboard_token")
   - async apiFetch(path): GET request to /api/dashboard{path} with Authorization: Bearer header. On 401: clearToken(), window.location.reload(). On non-ok: throw Error with status. Return parsed JSON.
   - Export all 4 functions.

4. **Create src/hooks/useInterval.js** -- Dan Abramov declarative setInterval hook:
   - Takes callback and delay (ms or null to pause).
   - Uses useRef for savedCallback (avoids stale closure).
   - useEffect to update ref, useEffect to manage setInterval lifecycle.
   - Export named: { useInterval }.

5. **Create src/constants.js** -- Shared constants:
   - KANBAN_COLUMNS: array of { key, label, statuses } for 6 columns:
     - new: ["new"], queued: ["queued"], in_progress: ["calling","in_call"], follow_up: ["follow_up"], committed: ["committed","payment_sent"], closed: ["declined","not_qualified","do_not_contact","failed"]
   - OUTCOME_COLORS: object mapping outcome strings to { bg, text, dot } Tailwind classes:
     - committed: green (bg-green-100, text-green-800, bg-green-500)
     - follow_up: yellow (bg-yellow-100, text-yellow-800, bg-yellow-500)
     - declined: red (bg-red-100, text-red-800, bg-red-500)
     - no_answer/voicemail/busy: gray (bg-gray-100, text-gray-600, bg-gray-400)
   - POLL_LIVE: 5000, POLL_PIPELINE: 30000, POLL_STRATEGY: 30000
   - maskPhone(phone): phone.slice(0,4) + "****" + phone.slice(-2), with null guard
   - formatDuration(seconds): mm:ss or hh:mm:ss formatting
   - formatTime(isoString): locale time string (short)

6. **Create src/components/EmptyState.jsx** -- Reusable empty state:
   - Props: icon (optional, default chart-like SVG), title, message
   - Renders centered container with muted icon, title text, description text
   - Tailwind: text-gray-400, centered, py-12, appropriate spacing
   - Default message: "No data yet. Analytics will appear after Sarah's first calls."

7. **Create src/components/OutcomeBadge.jsx** -- Color-coded outcome badge:
   - Props: outcome (string)
   - Looks up OUTCOME_COLORS[outcome] for styling, falls back to gray
   - Renders: small pill with dot indicator + capitalized outcome text
   - Tailwind: inline-flex, items-center, gap-1.5, px-2.5, py-0.5, rounded-full, text-xs, font-medium

8. **Run npm install** to install upgraded dependencies. Verify npm run build succeeds.
  </action>
  <verify>
    <automated>cd execution/dashboard && npm install --legacy-peer-deps 2>&1 | tail -3 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>package.json upgraded to React 19 + Vite 6 + Recharts 3. Shared infrastructure files (api.js, useInterval.js, constants.js, EmptyState.jsx, OutcomeBadge.jsx) created and importable. npm run build passes.</done>
</task>

</tasks>

<verification>
1. Backend: `cd execution/backend && python -c "from main import app; print('main.py imports OK')"` passes
2. Backend: 4 dashboard routes registered and protected by bearer token
3. Frontend: `cd execution/dashboard && npm run build` passes with zero errors
4. Frontend: All shared modules importable (api.js, useInterval.js, constants.js)
5. Column fix: main.py call_analyzed handler writes to "summary" key (not "call_summary")
</verification>

<success_criteria>
- 4 FastAPI dashboard endpoints return real Supabase data (not stubs)
- Dashboard project builds with React 19.x, Vite 6.x, Recharts 3.x, Tailwind 3.4.x
- Shared auth wrapper, polling hook, constants, EmptyState, and OutcomeBadge are available for Plans 02-04
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard/08-01-SUMMARY.md`
</output>
