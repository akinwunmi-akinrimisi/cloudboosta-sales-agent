---
phase: 08-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - execution/dashboard/src/components/Pipeline.jsx
  - execution/dashboard/src/components/KanbanColumn.jsx
  - execution/dashboard/src/components/LeadCard.jsx
  - execution/dashboard/src/components/LeadSidePanel.jsx
autonomous: true
requirements: [DASH-02, DASH-03]

must_haves:
  truths:
    - "Pipeline tab displays 6 kanban columns (New, Queued, In Progress, Follow-Up, Committed, Closed)"
    - "Each lead appears in the correct column based on status-to-column mapping"
    - "Lead cards show name, masked phone, time since last activity, retry count badge"
    - "Clicking a lead card opens a side panel sliding in from the right"
    - "Side panel shows full call history with transcripts and recording player"
    - "Pipeline data refreshes every 30 seconds"
    - "Empty state shown when no leads exist"
  artifacts:
    - path: "execution/dashboard/src/components/Pipeline.jsx"
      provides: "Pipeline tab with kanban board and side panel state"
      contains: "useInterval"
    - path: "execution/dashboard/src/components/KanbanColumn.jsx"
      provides: "Single kanban column with header count and lead cards"
    - path: "execution/dashboard/src/components/LeadCard.jsx"
      provides: "Compact lead card for kanban"
      contains: "maskPhone"
    - path: "execution/dashboard/src/components/LeadSidePanel.jsx"
      provides: "Slide-in detail panel with call history"
      contains: "audio"
  key_links:
    - from: "execution/dashboard/src/components/Pipeline.jsx"
      to: "/api/dashboard/pipeline"
      via: "apiFetch('/pipeline')"
      pattern: "apiFetch.*pipeline"
    - from: "execution/dashboard/src/components/LeadSidePanel.jsx"
      to: "/api/dashboard/lead/{id}"
      via: "apiFetch('/lead/' + leadId)"
      pattern: "apiFetch.*lead"
    - from: "execution/dashboard/src/components/Pipeline.jsx"
      to: "execution/dashboard/src/constants.js"
      via: "KANBAN_COLUMNS import"
      pattern: "import.*KANBAN_COLUMNS"
---

<objective>
Build the Pipeline tab with a 6-column kanban board displaying leads grouped by status, lead cards with essential info, and a slide-in side panel showing full call history, transcripts, and recording playback when a lead is clicked.

Purpose: The Pipeline tab gives the operator instant visibility into where every lead stands in the sales pipeline. The side panel provides deep-dive capability for reviewing call outcomes and transcripts without leaving the dashboard.

Output: Pipeline.jsx (kanban orchestrator), KanbanColumn.jsx, LeadCard.jsx, LeadSidePanel.jsx.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard/08-CONTEXT.md
@.planning/phases/08-dashboard/08-RESEARCH.md
@.planning/phases/08-dashboard/08-01-SUMMARY.md

@execution/dashboard/src/api.js
@execution/dashboard/src/hooks/useInterval.js
@execution/dashboard/src/constants.js
@execution/dashboard/src/components/EmptyState.jsx
@execution/dashboard/src/components/OutcomeBadge.jsx

<interfaces>
<!-- From Plan 01 (constants.js) -->
export const KANBAN_COLUMNS = [
  { key: "new", label: "New", statuses: ["new"] },
  { key: "queued", label: "Queued", statuses: ["queued"] },
  { key: "in_progress", label: "In Progress", statuses: ["calling", "in_call"] },
  { key: "follow_up", label: "Follow-Up", statuses: ["follow_up"] },
  { key: "committed", label: "Committed", statuses: ["committed", "payment_sent"] },
  { key: "closed", label: "Closed", statuses: ["declined", "not_qualified", "do_not_contact", "failed"] },
]
export const OUTCOME_COLORS: { ... }
export function maskPhone(phone: string): string
export function formatDuration(seconds: number): string

<!-- From Plan 01 (api.js) -->
export async function apiFetch(path: string): Promise<any>

<!-- Backend /api/dashboard/pipeline response -->
{ leads: [{ id, name, phone, status, updated_at, retry_count, programme_recommended, last_call_at, outcome, priority }] }

<!-- Backend /api/dashboard/lead/{id} response -->
{
  lead: { id, name, phone, email, location, country, status, ... all leads columns },
  calls: [{ id, lead_id, retell_call_id, started_at, ended_at, duration_seconds, outcome, closing_strategy_used, detected_persona, transcript, recording_url, summary, ... }]
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline kanban board with 6 columns and lead cards</name>
  <files>execution/dashboard/src/components/Pipeline.jsx, execution/dashboard/src/components/KanbanColumn.jsx, execution/dashboard/src/components/LeadCard.jsx</files>
  <action>
1. **Create src/components/LeadCard.jsx:**
   - Props: lead (object), onClick (callback)
   - Render a compact card (bg-white dark:bg-gray-800, rounded, border, p-3, cursor-pointer, hover:shadow-md transition-shadow)
   - Content:
     - Lead name (font-medium, truncate)
     - Masked phone (text-sm text-gray-500, use maskPhone)
     - Bottom row: time since last activity (use relative time: compute from lead.updated_at, show "Xm ago", "Xh ago", "Xd ago") + retry count badge if retry_count > 0 (small rounded-full bg-orange-100 text-orange-700 px-1.5 text-xs showing retry_count)
   - OutcomeBadge if lead.outcome exists
   - onClick: call onClick(lead.id)

2. **Create src/components/KanbanColumn.jsx:**
   - Props: label (string), leads (array), count (number), colorClass (optional accent color), onLeadClick (callback)
   - Render a vertical column:
     - Header: label text + count badge (bg-gray-200 rounded-full px-2 text-xs)
     - Column accent: thin top border using colorClass (e.g., border-t-2 border-blue-500)
     - Scrollable body: max-h-[calc(100vh-280px)] overflow-y-auto, space-y-2
     - Map leads to LeadCard components, pass onLeadClick
   - Column colors by key (passed from parent): new=blue, queued=indigo, in_progress=amber, follow_up=yellow, committed=green, closed=gray

3. **Rewrite src/components/Pipeline.jsx** (replace stub entirely):
   - Import apiFetch, useInterval, POLL_PIPELINE, KANBAN_COLUMNS, KanbanColumn, LeadSidePanel, EmptyState
   - State: leads (array, default []), selectedLeadId (string|null), error (null)
   - fetchPipeline: async useCallback, calls apiFetch("/pipeline"), sets leads from response.leads
   - Initial fetch + polling: useInterval(fetchPipeline, POLL_PIPELINE) + useEffect initial call
   - Group leads into columns: for each KANBAN_COLUMNS entry, filter leads where lead.status is in column.statuses
   - Layout:
     - If all columns empty: render EmptyState with title "No leads in pipeline" message "Import leads via CSV to get started."
     - Otherwise: horizontal scrollable container (flex gap-4 overflow-x-auto pb-4)
     - 6 KanbanColumn components with grouped leads, pass column colors
   - Side panel: if selectedLeadId is not null, render LeadSidePanel with leadId={selectedLeadId} and onClose={() => setSelectedLeadId(null)}
   - Column color mapping: { new: "border-blue-500", queued: "border-indigo-500", in_progress: "border-amber-500", follow_up: "border-yellow-500", committed: "border-green-500", closed: "border-gray-400" }
   - Each column has min-w-[250px] flex-shrink-0 for consistent widths
  </action>
  <verify>
    <automated>cd execution/dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Pipeline tab shows 6 kanban columns with leads grouped by status. Lead cards display name, masked phone, relative time, and retry badge. Clicking a lead sets selectedLeadId for side panel. Data polls every 30 seconds. Empty state shows when no leads exist.</done>
</task>

<task type="auto">
  <name>Task 2: Lead side panel with call history, transcript, and recording player</name>
  <files>execution/dashboard/src/components/LeadSidePanel.jsx</files>
  <action>
Create src/components/LeadSidePanel.jsx:

- Props: leadId (string), onClose (callback)
- State: data (null), loading (true), error (null)
- On mount / leadId change: useEffect fetches apiFetch("/lead/" + leadId). Sets data = response (has lead + calls), loading = false. On error: set error message.
- Layout: Fixed panel sliding in from right side of viewport
  - Overlay: fixed inset-0 bg-black/30 z-40, onClick={onClose} (clicking outside closes panel)
  - Panel: fixed right-0 top-0 h-full w-full max-w-lg bg-white dark:bg-gray-900 shadow-xl z-50, overflow-y-auto
  - Transform animation: Use Tailwind translate-x-0 (open) transition-transform duration-300
  - Header: sticky top-0, lead name + close button (X icon), masked phone, status badge, lead email (if exists)

- Lead detail section (below header):
  - Location/country
  - Programme recommended
  - Outcome (with OutcomeBadge)
  - Priority, retry count
  - Source

- Call history section:
  - Heading: "Call History" with count
  - If no calls: "No call records yet"
  - For each call in data.calls (sorted desc by started_at):
    - Call card: date/time, duration (formatDuration), outcome badge, strategy used, persona detected
    - Transcript: collapsible section. Show first 200 chars with "Show full transcript" toggle. Full transcript in pre/whitespace-pre-wrap block with text-sm.
    - Recording player: if call.recording_url exists, render <audio controls src={call.recording_url} className="w-full" />. If no recording_url: "No recording available" muted text.
    - Summary: call.summary if available

- Loading state: centered spinner/skeleton while fetching
- Error state: error message with retry button

- Close on Escape key: useEffect with keydown listener for "Escape" key -> onClose
  </action>
  <verify>
    <automated>cd execution/dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Side panel slides in from right when a lead is clicked. Shows lead details, full call history with expandable transcripts, recording player, and strategy/outcome info. Closes on overlay click or Escape key. Loading and error states handled.</done>
</task>

</tasks>

<verification>
1. `cd execution/dashboard && npm run build` passes with zero errors
2. Pipeline.jsx groups leads into 6 KANBAN_COLUMNS correctly
3. LeadCard renders name, masked phone, relative time, retry badge
4. LeadSidePanel fetches /api/dashboard/lead/{id} on open
5. Side panel includes transcript viewer and audio recording player
6. Pipeline polls /api/dashboard/pipeline every 30 seconds
</verification>

<success_criteria>
- 6 kanban columns display with leads grouped by status
- Lead cards are compact and scannable with key info
- Clicking a lead opens side panel with full details and call history
- Transcripts are expandable, recordings playable via HTML5 audio
- Data refreshes every 30 seconds
- Empty state displayed when no leads exist
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard/08-03-SUMMARY.md`
</output>
