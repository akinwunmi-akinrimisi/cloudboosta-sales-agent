---
phase: 01-foundation-sdk-migration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - execution/backend/schema/004_views.sql
  - execution/backend/schema/005_functions.sql
  - execution/backend/seeds/001_programmes.sql
  - execution/backend/seeds/002_pricing.sql
  - execution/backend/seeds/003_objection_responses.sql
  - execution/backend/seeds/004_test_leads.sql
  - execution/backend/test_phase1.py
autonomous: true
requirements: [DATA-05, DATA-06]

must_haves:
  truths:
    - "pipeline_snapshot view returns lead counts grouped by status"
    - "strategy_performance view returns conversion rates per closing strategy"
    - "todays_calls view returns call_logs from the current UTC day"
    - "pick_next_lead() RPC atomically selects highest-priority queued lead and sets status to calling"
    - "Seed data populates 4 programmes, 16 pricing rows, 30+ objection responses, and 10 test leads"
    - "test_phase1.py validates all DATA requirements against the live database"
  artifacts:
    - path: "execution/backend/schema/004_views.sql"
      provides: "3 SQL views for dashboard queries"
      contains: "CREATE.*VIEW.*pipeline_snapshot"
    - path: "execution/backend/schema/005_functions.sql"
      provides: "pick_next_lead() RPC function (appended to existing file)"
      contains: "pick_next_lead"
    - path: "execution/backend/seeds/001_programmes.sql"
      provides: "4 Cloudboosta pathway seed data"
      contains: "INSERT INTO programmes"
    - path: "execution/backend/seeds/002_pricing.sql"
      provides: "16 pricing rows (4 bundles x 4 currencies)"
      contains: "INSERT INTO pricing"
    - path: "execution/backend/seeds/003_objection_responses.sql"
      provides: "30+ objection handling seed data"
      contains: "INSERT INTO objection_responses"
    - path: "execution/backend/seeds/004_test_leads.sql"
      provides: "10 Wave 0 test leads"
      contains: "INSERT INTO leads"
    - path: "execution/backend/test_phase1.py"
      provides: "Automated verification of all Phase 1 DATA requirements"
      contains: "def test_"
  key_links:
    - from: "execution/backend/schema/004_views.sql"
      to: "execution/backend/schema/001_tables.sql"
      via: "Views query leads, call_logs, pipeline_logs tables"
      pattern: "FROM leads|FROM call_logs|FROM pipeline_logs"
    - from: "execution/backend/schema/005_functions.sql"
      to: "execution/backend/schema/001_tables.sql"
      via: "pick_next_lead() queries and updates leads table with FOR UPDATE SKIP LOCKED"
      pattern: "FOR UPDATE SKIP LOCKED"
    - from: "execution/backend/seeds/"
      to: "execution/backend/schema/001_tables.sql"
      via: "Seed files INSERT into tables created by 001_tables.sql"
      pattern: "INSERT INTO (programmes|pricing|objection_responses|leads)"
---

<objective>
Create SQL views for dashboard queries, the atomic pick_next_lead() RPC function, all seed data files, and an automated test script to validate every Phase 1 database requirement.

Purpose: Views provide pre-computed queries for the dashboard (Phase 8). pick_next_lead() is the core queue-picking mechanism for the auto-dialer (Phase 6). Seed data populates reference tables and test leads for all subsequent phases. The test script provides automated verification.
Output: 1 view file, appended RPC function, 4 seed files, and 1 test script.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-sdk-migration/01-CONTEXT.md
@.planning/phases/01-foundation-sdk-migration/01-RESEARCH.md
@.planning/phases/01-foundation-sdk-migration/01-01-SUMMARY.md

@execution/backend/supabase_client.py
@execution/backend/dialer.py

<interfaces>
<!-- From Plan 01 schema files -- tables that views/RPC/seeds depend on -->

Tables created by 001_tables.sql:
- leads (id UUID PK, name TEXT, phone TEXT UNIQUE, status TEXT with 14-state CHECK, priority INTEGER, created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ, ...)
- call_logs (id UUID PK, lead_id UUID FK->leads, retell_call_id TEXT UNIQUE, outcome TEXT, closing_strategy_used TEXT, detected_persona TEXT, started_at TIMESTAMPTZ, duration_seconds INTEGER, ...)
- pipeline_logs (id UUID PK, lead_id UUID FK->leads, component TEXT, event TEXT, details JSONB, created_at TIMESTAMPTZ)
- dial_schedules (id UUID PK, name TEXT, start_time TIME, end_time TIME, timezone TEXT, days_of_week INTEGER[], is_active BOOLEAN, ...)
- programmes (id UUID PK, name TEXT UNIQUE, slug TEXT UNIQUE, duration_weeks INTEGER, topics TEXT[], tools TEXT[], roles_after TEXT[], ...)
- pricing (id UUID PK, bundle_name TEXT, bundle_slug TEXT, bundle_size INTEGER, currency TEXT CHECK IN GBP/USD/EUR/NGN, standard_price DECIMAL, early_bird_price DECIMAL, UNIQUE(bundle_slug, currency), ...)
- objection_responses (id UUID PK, category TEXT, objection_key TEXT UNIQUE, trigger_phrases TEXT[], responses JSONB, cultural_nuances JSONB, recovery_script TEXT, ...)

Triggers from 005_functions.sql:
- trg_enforce_lead_status (BEFORE UPDATE OF status ON leads) -- validates transitions
- trg_log_status_transition (AFTER UPDATE OF status ON leads) -- auto-inserts pipeline_logs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL views and pick_next_lead() RPC function</name>
  <files>execution/backend/schema/004_views.sql, execution/backend/schema/005_functions.sql</files>
  <action>
**004_views.sql** -- 3 SQL views (DATA-05):

1. **pipeline_snapshot** -- Lead counts by status for dashboard Pipeline tab:
```sql
CREATE OR REPLACE VIEW pipeline_snapshot AS
SELECT
  status,
  COUNT(*) as count,
  MIN(created_at) as oldest,
  MAX(updated_at) as latest
FROM leads
GROUP BY status
ORDER BY CASE status
  WHEN 'new' THEN 1
  WHEN 'queued' THEN 2
  WHEN 'calling' THEN 3
  WHEN 'in_call' THEN 4
  WHEN 'committed' THEN 5
  WHEN 'follow_up' THEN 6
  WHEN 'payment_sent' THEN 7
  WHEN 'declined' THEN 8
  WHEN 'not_qualified' THEN 9
  WHEN 'no_answer' THEN 10
  WHEN 'voicemail' THEN 11
  WHEN 'busy' THEN 12
  WHEN 'do_not_contact' THEN 13
  WHEN 'failed' THEN 14
END;
```

2. **strategy_performance** -- Conversion rates by closing strategy for Strategy Analytics tab:
```sql
CREATE OR REPLACE VIEW strategy_performance AS
SELECT
  closing_strategy_used AS strategy,
  COUNT(*) AS total_calls,
  COUNT(*) FILTER (WHERE outcome = 'committed') AS committed_count,
  ROUND(
    COUNT(*) FILTER (WHERE outcome = 'committed') * 100.0 / NULLIF(COUNT(*), 0),
    1
  ) AS conversion_rate,
  COUNT(DISTINCT detected_persona) AS personas_seen
FROM call_logs
WHERE closing_strategy_used IS NOT NULL
GROUP BY closing_strategy_used
ORDER BY conversion_rate DESC NULLS LAST;
```

3. **todays_calls** -- Today's calls for Live View tab:
```sql
CREATE OR REPLACE VIEW todays_calls AS
SELECT
  cl.id, cl.lead_id, cl.retell_call_id,
  cl.started_at, cl.ended_at, cl.duration_seconds,
  cl.outcome, cl.closing_strategy_used, cl.detected_persona,
  cl.summary, cl.recording_url,
  l.name AS lead_name, l.phone AS lead_phone
FROM call_logs cl
LEFT JOIN leads l ON cl.lead_id = l.id
WHERE cl.started_at >= CURRENT_DATE
ORDER BY cl.started_at DESC;
```

Use CREATE OR REPLACE VIEW for idempotency.

**005_functions.sql** -- APPEND the pick_next_lead() function to the existing file created by Plan 01 (DATA-06):

The pick_next_lead() RPC function uses FOR UPDATE SKIP LOCKED for atomic queue picking:
- SELECT the highest-priority queued lead (ORDER BY priority DESC, created_at ASC)
- Lock it with FOR UPDATE SKIP LOCKED (prevents race conditions)
- UPDATE status to 'calling' and updated_at to NOW()
- RETURN the lead row with updated status
- If no queued leads, RETURN empty set (not an error)
- LANGUAGE plpgsql, RETURNS SETOF leads
- Include DROP FUNCTION IF EXISTS pick_next_lead() before CREATE for idempotency

Use the exact pattern from RESEARCH.md Pattern 1. This function will be called by the auto-dialer (Phase 6) via supabase.rpc("pick_next_lead").execute().
  </action>
  <verify>
    <automated>cd execution/backend && grep -c "CREATE.*VIEW" schema/004_views.sql | grep -q "3" && grep "pick_next_lead" schema/005_functions.sql > /dev/null && grep "FOR UPDATE SKIP LOCKED" schema/005_functions.sql > /dev/null && echo "PASS: 3 views + RPC" || echo "FAIL"</automated>
  </verify>
  <done>3 SQL views (pipeline_snapshot, strategy_performance, todays_calls) created with correct aggregation logic. pick_next_lead() RPC function appended to 005_functions.sql using FOR UPDATE SKIP LOCKED pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Create seed data files and automated test script</name>
  <files>execution/backend/seeds/001_programmes.sql, execution/backend/seeds/002_pricing.sql, execution/backend/seeds/003_objection_responses.sql, execution/backend/seeds/004_test_leads.sql, execution/backend/test_phase1.py</files>
  <action>
Create `execution/backend/seeds/` directory and 4 seed files, plus a test script.

**001_programmes.sql** -- 4 Cloudboosta pathways (from CONTEXT.md):
- Cloud Computing (slug: cloud-computing, 8 weeks)
- Advanced DevOps (slug: advanced-devops, 8 weeks)
- Platform Engineer (slug: platform-engineer, 8 weeks)
- SRE (slug: sre, 8 weeks)
Each pathway: populate description, topics array, prerequisites, tools array, roles_after array. Extract from knowledge-base context. Use display_order 1-4. Mark all as is_active=TRUE.

**002_pricing.sql** -- 16 rows = 4 bundles x 4 currencies (from PROJECT.md authoritative pricing):
Bundles (per CONTEXT.md):
- Zero to Cloud DevOps (pathways 1+2, bundle_size=2, 16 weeks)
- DevOps Pro (pathways 3+4, bundle_size=2, 16 weeks)
- 3 Pathways (any 3 pathways, bundle_size=3, 24 weeks)
- Zero to DevOps Pro (all 4, bundle_size=4, 32 weeks)

GBP pricing (authoritative from PROJECT.md):
- 1 pathway (8 wks): 1500 standard / 1350 early bird -- NOTE: This is per-pathway, not per-bundle. The 4 bundles start at 2-pathway.
  Wait -- PROJECT.md says: 1 pathway (8 wks): 1500/1350, 2 pathways (16 wks): 3000/2400, 3 pathways (24 wks): 4500/3450, all 4 (32 wks): 6000/4500.
  But CONTEXT.md says bundle tracks are: Zero to Cloud DevOps (1+2), DevOps Pro (3+4), 3 Pathways (any 3), Zero to DevOps Pro (all 4).
  These bundles correspond to: 2-pathway, 2-pathway, 3-pathway, 4-pathway pricing.
  ALSO need individual pathway pricing (bundle_size=1). So actually 5 bundles x 4 currencies = 20 rows. But CONTEXT.md explicitly says "16 rows = 4 bundles x 4 currencies". Follow the CONTEXT.md spec: 4 bundles only. The individual pathway pricing can be derived from the 2-pathway bundle.

  Actually re-reading CONTEXT.md more carefully: "4 bundle tracks: Zero to Cloud DevOps (1+2), DevOps Pro (3+4), 3 Pathways (any 3), Zero to DevOps Pro (all 4)". These are the 4 bundles. Pricing for these bundles maps to the PROJECT.md tiers:
  - Zero to Cloud DevOps (2 pathways): GBP 3000 / 2400 early bird
  - DevOps Pro (2 pathways): GBP 3000 / 2400 early bird
  - 3 Pathways (3 pathways): GBP 4500 / 3450 early bird
  - Zero to DevOps Pro (all 4): GBP 6000 / 4500 early bird

For USD, EUR, NGN: Use approximate conversion rates.
  - USD: multiply GBP by 1.27 (round to nearest 50)
  - EUR: multiply GBP by 1.17 (round to nearest 50)
  - NGN: multiply GBP by 2000 (round to nearest 10000)

Early bird deadline: 2026-03-18. Cohort start: 2026-04-25.
Instalment info (per CONTEXT.md): 2 instalments adds 100 total, 3 instalments adds 200 total. Payment dates: 2026-03-30, 2026-04-30, 2026-05-30.
- instalment_2_total = standard_price + 100 (in respective currency equivalent)
- instalment_3_total = standard_price + 200 (in respective currency equivalent)
- instalment_dates = ARRAY['2026-03-30', '2026-04-30', '2026-05-30']

Use ON CONFLICT (bundle_slug, currency) DO NOTHING for idempotency.

**003_objection_responses.sql** -- 30+ objection rows (from CONTEXT.md 10 categories):
Categories: Price & Money, Time & Commitment, Trust & Credibility, Personal & Family, Self-Doubt & Fear, Market & Career Doubts, Logistics & Format, Competitor & Alternative, Stalls & Deflections, Compound & Edge Cases.

Create 3+ objections per category (30+ total). For each objection:
- category: one of the 10 categories
- objection_key: snake_case unique identifier (e.g., "price_too_expensive", "no_time_family")
- trigger_phrases: TEXT[] of 3-5 common phrases leads say (e.g., ARRAY['too expensive', 'can''t afford it', 'out of my budget'])
- what_theyre_saying: Brief explanation of what the lead really means
- responses: JSONB array of 2-3 response objects, each with {label, script} -- e.g., [{"label": "Empathize", "script": "I completely understand..."}, {"label": "Reframe", "script": "Let me share..."}]
- cultural_nuances: JSONB with {nigeria, uk, us} keys containing culture-specific advice
- recovery_script: What to say if the objection persists after all responses
- escalation_trigger: When to back off (e.g., "If lead says 'I said no' twice, move to graceful close")

Write realistic, sales-professional responses. These will be used by Sarah during live calls. Use ON CONFLICT (objection_key) DO NOTHING for idempotency.

**004_test_leads.sql** -- 10 Wave 0 test leads:
Create 10 leads with realistic but clearly test data:
- Names: "Test Lead 01" through "Test Lead 10"
- Phones: Use +1555XXXXXXX format (US test numbers)
- Mix of countries: UK (3), US (3), Nigeria (2), Germany (1), Canada (1)
- Currency matching country
- All status='new' initially
- Priority 0-5 (varied)
- source='wave_0_test'

Use ON CONFLICT (phone) DO NOTHING for idempotency.

**test_phase1.py** -- Automated validation script:
A Python script that connects to Supabase using supabase_client and validates all DATA requirements:

1. DATA-01: Verify leads table exists, insert a test lead with valid status, verify invalid status rejected
2. DATA-02: Verify call_logs table exists, verify retell_call_id UNIQUE constraint
3. DATA-03: Insert a lead, update its status from 'new' to 'queued', verify pipeline_logs row was auto-created
4. DATA-04: Verify dial_schedules table exists and has correct columns
5. DATA-05: Verify pipeline_snapshot, strategy_performance, todays_calls views exist and return data
6. DATA-06: Insert a 'queued' lead, call pick_next_lead() RPC, verify it returns the lead with status='calling'
7. DATA-07: Print reminder to test with anon key separately (cannot test both keys in same script without two clients)

Each test: try/except with clear PASS/FAIL output. Clean up test data after each test (DELETE inserted rows). Exit with code 0 if all pass, 1 if any fail.

Script must be runnable from execution/backend/ directory: `python test_phase1.py`
Use existing supabase_client.py for connection.
  </action>
  <verify>
    <automated>cd execution/backend && ls seeds/001_programmes.sql seeds/002_pricing.sql seeds/003_objection_responses.sql seeds/004_test_leads.sql test_phase1.py > /dev/null 2>&1 && grep -c "INSERT INTO" seeds/001_programmes.sql | grep -q "[4-9]" && grep -c "INSERT INTO" seeds/004_test_leads.sql | grep -q "1[0-9]\|[2-9][0-9]" && echo "PASS: All seed files + test script exist" || echo "FAIL"</automated>
  </verify>
  <done>4 seed files created: 4 programmes, 16 pricing rows (4 bundles x 4 currencies with instalment info), 30+ objection responses across 10 categories, 10 test leads. test_phase1.py validates all DATA-01 through DATA-07 requirements with clear PASS/FAIL output and cleanup.</done>
</task>

</tasks>

<verification>
- pipeline_snapshot view returns correct status counts after seed data
- strategy_performance view works (returns empty since no call_logs yet, but no SQL error)
- todays_calls view works (returns empty, no error)
- pick_next_lead() RPC selects highest-priority queued lead and changes status to 'calling'
- pick_next_lead() returns empty set when no queued leads exist
- All 4 seed files run without errors after schema is applied
- test_phase1.py runs and all tests pass against live Supabase
</verification>

<success_criteria>
- 3 SQL views exist and return data without errors
- pick_next_lead() RPC function works atomically with FOR UPDATE SKIP LOCKED
- 4 seed SQL files are valid and populate correct row counts (4 + 16 + 30+ + 10)
- test_phase1.py validates DATA-01 through DATA-07 with automated PASS/FAIL
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-sdk-migration/01-02-SUMMARY.md`
</output>
