---
phase: 06-auto-dialer-retry-logic
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - execution/n8n/auto-dialer.json
autonomous: false
requirements: [AUTO-01, AUTO-06]

must_haves:
  truths:
    - "n8n workflow polls the lead queue every 2 minutes via cron schedule"
    - "Workflow checks dial window (Europe/London timezone) and stops silently if outside hours"
    - "Workflow checks for active call (status calling/in_call) and stops if one exists"
    - "Workflow calls pick_next_lead() RPC via HTTP Request (not Supabase node) and stops if queue is empty"
    - "Workflow POSTs to /retell/initiate-call with bearer auth and the picked lead_id"
    - "On 409/429 response, lead is reverted to 'queued'; on 500, lead is set to 'failed'"
    - "DNC enforcement is NOT in the workflow -- handled by database and backend only"
  artifacts:
    - path: "execution/n8n/auto-dialer.json"
      provides: "Complete n8n workflow JSON for auto-dialer"
      contains: "scheduleTrigger"
  key_links:
    - from: "execution/n8n/auto-dialer.json (Pick Next Lead node)"
      to: "Supabase REST API /rest/v1/rpc/pick_next_lead"
      via: "HTTP Request POST with apikey header"
      pattern: "rpc/pick_next_lead"
    - from: "execution/n8n/auto-dialer.json (Initiate Call node)"
      to: "FastAPI /retell/initiate-call"
      via: "HTTP Request POST with Bearer token"
      pattern: "retell/initiate-call"
---

<objective>
Create the n8n auto-dialer workflow JSON that polls the lead queue every 2 minutes, checks dial windows and active calls, picks the next lead via Supabase RPC, and initiates calls through the FastAPI backend with proper error handling.

Purpose: This workflow is the autonomous calling engine -- it runs unattended, making calls within safe dial windows, and gracefully handles errors by reverting lead status.

Output: Complete execution/n8n/auto-dialer.json ready for import into the n8n instance at n8n.srv1297445.hstgr.cloud.
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auto-dialer-retry-logic/06-CONTEXT.md
@.planning/phases/06-auto-dialer-retry-logic/06-RESEARCH.md
@.planning/phases/06-auto-dialer-retry-logic/06-01-SUMMARY.md

<interfaces>
<!-- Key contracts from research that executor needs for n8n JSON construction -->

From 06-RESEARCH.md (n8n node types and versions -- MUST use these exact types):
- Schedule Trigger: type "n8n-nodes-base.scheduleTrigger", typeVersion 1.2
- Supabase node: type "n8n-nodes-base.supabase", typeVersion 1
- HTTP Request: type "n8n-nodes-base.httpRequest", typeVersion 4.2
- IF node: type "n8n-nodes-base.if", typeVersion 2
- Code node: type "n8n-nodes-base.code", typeVersion 2

From 06-RESEARCH.md (connection format for IF node branches):
```json
{
  "IF Node Name": {
    "main": [
      [{ "node": "True Branch Target", "type": "main", "index": 0 }],
      [{ "node": "False Branch Target", "type": "main", "index": 0 }]
    ]
  }
}
```
First sub-array = true branch (condition met), second sub-array = false branch.

From 06-RESEARCH.md (critical: n8n Supabase node CANNOT reliably call RPCs):
Use HTTP Request node to POST to $env.SUPABASE_URL/rest/v1/rpc/pick_next_lead
with headers: apikey=$env.SUPABASE_SERVICE_KEY, Authorization=Bearer $env.SUPABASE_SERVICE_KEY

From 06-CONTEXT.md (locked decisions):
- Cron: */2 * * * *
- Error handling: 409/429 -> revert to queued, 500 -> set to failed
- DNC: backend only, NOT in n8n workflow
- Credentials: Supabase service key + DASHBOARD_SECRET_KEY for bearer auth
- Dial window: use Luxon (built into n8n) for timezone-aware time comparison

From execution/backend/main.py (initiate-call endpoint):
- POST /retell/initiate-call
- Body: {"lead_id": "<uuid>"}
- Header: Authorization: Bearer <DASHBOARD_SECRET_KEY>
- Returns: 200 success, 409 active call, 429 rate limited, 403 DNC/declined, 500 error

From 06-RESEARCH.md (required n8n env vars -- use $env references):
- SUPABASE_URL: Supabase project URL
- SUPABASE_SERVICE_KEY: Supabase service role key
- WEBHOOK_BASE_URL: FastAPI server base URL
- DASHBOARD_SECRET_KEY: Bearer token for initiate-call
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete n8n auto-dialer workflow JSON</name>
  <files>execution/n8n/auto-dialer.json</files>
  <action>
Replace the placeholder execution/n8n/auto-dialer.json with the complete workflow. The workflow must contain these nodes connected in sequence, with branching for error handling:

**Node 1: "Every 2 Minutes" (Schedule Trigger)**
- type: n8n-nodes-base.scheduleTrigger, typeVersion 1.2
- Cron expression: */2 * * * *
- Position: [0, 0]

**Node 2: "Get Active Schedules" (Supabase getAll)**
- type: n8n-nodes-base.supabase, typeVersion 1
- Table: dial_schedules, filter: is_active eq true, limit 10
- Credentials: supabaseApi with placeholder id "SUPABASE_CRED_ID" and name "Supabase Service Key"
- Position: [250, 0]

**Node 3: "Check Dial Window" (Code node)**
- type: n8n-nodes-base.code, typeVersion 2
- JavaScript using Luxon (built into n8n):
  - Get all schedule items from $input.all()
  - For each schedule: get timezone (default Europe/London), get current time in that timezone using DateTime.now().setZone(tz)
  - Compare currentTime (HH:mm format) against start_time and end_time
  - Check day_of_week includes current weekday (Luxon weekday: 1=Mon, 7=Sun)
  - If any schedule matches: return [{json: {inWindow: true}}]
  - If no match: return [] (empty = stops workflow execution)
- Position: [500, 0]

**Node 4: "Check Active Call" (Supabase getAll)**
- type: n8n-nodes-base.supabase, typeVersion 1
- Table: leads, filter: status in "calling,in_call", limit 1
- Same credentials as Node 2
- Position: [750, 0]

**Node 5: "Has Active Call?" (IF node)**
- type: n8n-nodes-base.if, typeVersion 2
- Condition: $json.id is NOT empty (check if any result returned)
- TRUE branch (has active call) -> Node: "No-Op Active" (stop, do nothing)
- FALSE branch (no active call) -> continue to Pick Next Lead
- Position: [950, 0]

**Node 6: "No-Op Active" (No Operation)**
- type: n8n-nodes-base.noOp, typeVersion 1
- Position: [1150, -150]
- (Dead end -- stops this branch)

**Node 7: "Pick Next Lead (RPC)" (HTTP Request)**
- type: n8n-nodes-base.httpRequest, typeVersion 4.2
- Method: POST
- URL: ={{ $env.SUPABASE_URL }}/rest/v1/rpc/pick_next_lead
- Headers: apikey = {{ $env.SUPABASE_SERVICE_KEY }}, Authorization = Bearer {{ $env.SUPABASE_SERVICE_KEY }}, Content-Type = application/json
- Body: empty JSON object {} (RPC takes no params)
- Options: fullResponse = true (to get status code + body)
- Position: [1150, 150]

**Node 8: "Has Lead?" (IF node)**
- type: n8n-nodes-base.if, typeVersion 2
- Condition: check that response body is not empty array -- use expression {{ $json.body.length }} > 0
- TRUE branch (has lead) -> Initiate Call
- FALSE branch (empty queue) -> Node: "No-Op Empty" (stop)
- Position: [1350, 150]

**Node 9: "No-Op Empty" (No Operation)**
- type: n8n-nodes-base.noOp, typeVersion 1
- Position: [1550, 0]

**Node 10: "Initiate Call" (HTTP Request)**
- type: n8n-nodes-base.httpRequest, typeVersion 4.2
- Method: POST
- URL: ={{ $env.WEBHOOK_BASE_URL }}/retell/initiate-call
- Headers: Authorization = Bearer {{ $env.DASHBOARD_SECRET_KEY }}, Content-Type = application/json
- Body (JSON): { "lead_id": "={{ $json.body[0].id }}" }
- Options: fullResponse = true, ignoreHttpStatusErrors = true (to handle 409/429/500 without crashing)
- Position: [1550, 300]

**Node 11: "Call Response Check" (IF node)**
- type: n8n-nodes-base.if, typeVersion 2
- Condition: {{ $json.statusCode }} >= 200 AND {{ $json.statusCode }} < 300
- TRUE branch -> Node: "No-Op Success" (done, call initiated)
- FALSE branch -> "Is Server Error?" node
- Position: [1750, 300]

**Node 12: "No-Op Success" (No Operation)**
- type: n8n-nodes-base.noOp, typeVersion 1
- Position: [1950, 200]

**Node 13: "Is Server Error?" (IF node)**
- type: n8n-nodes-base.if, typeVersion 2
- Condition: {{ $json.statusCode }} >= 500
- TRUE branch (500) -> "Set Failed" (Supabase update leads SET status='failed')
- FALSE branch (409/429) -> "Revert to Queued" (Supabase update leads SET status='queued')
- Position: [1950, 450]

**Node 14: "Set Failed" (Supabase update)**
- type: n8n-nodes-base.supabase, typeVersion 1
- Operation: update, table: leads
- Match column: id, match value: the lead id from RPC result (use expression referencing Pick Next Lead node output: {{ $('Pick Next Lead (RPC)').item.json.body[0].id }})
- Update fields: status = "failed"
- Same credentials
- Position: [2150, 350]

**Node 15: "Revert to Queued" (Supabase update)**
- type: n8n-nodes-base.supabase, typeVersion 1
- Operation: update, table: leads
- Match column: id, match value: lead id from RPC result (same expression as Node 14)
- Update fields: status = "queued"
- Same credentials
- Position: [2150, 550]

**Connections map:**
- Every 2 Minutes -> Get Active Schedules -> Check Dial Window -> Check Active Call -> Has Active Call?
- Has Active Call? TRUE -> No-Op Active (stop)
- Has Active Call? FALSE -> Pick Next Lead (RPC) -> Has Lead?
- Has Lead? TRUE -> Initiate Call -> Call Response Check
- Has Lead? FALSE -> No-Op Empty (stop)
- Call Response Check TRUE -> No-Op Success (done)
- Call Response Check FALSE -> Is Server Error?
- Is Server Error? TRUE -> Set Failed
- Is Server Error? FALSE -> Revert to Queued

**Top-level JSON structure:**
- name: "Sarah Auto-Dialer"
- nodes: array of all node objects above
- connections: object mapping node names to their outputs per the connection format
- active: false (user activates after import and credential linking)
- settings: { executionOrder: "v1" }
- tags: ["sarah", "auto-dialer"]

**Credential references:** Use placeholder id "SUPABASE_CRED_ID" and name "Supabase Service Key" for all Supabase nodes. The HTTP Request nodes use $env references directly (no credential objects needed for header-based auth).

**IMPORTANT:** Do NOT include any DNC check in the workflow. DNC enforcement is handled by pick_next_lead() (only selects status='queued') and initiate-call endpoint (returns 403 for DNC). This is a locked user decision.
  </action>
  <verify>
    <automated>python -c "import json; w=json.load(open('execution/n8n/auto-dialer.json')); nodes=[n['name'] for n in w['nodes']]; assert len(w['nodes']) >= 12, f'Expected 12+ nodes, got {len(w[\"nodes\"])}'; assert 'Every 2 Minutes' in nodes; assert 'Pick Next Lead (RPC)' in nodes; assert 'Initiate Call' in nodes; assert 'Revert to Queued' in nodes; assert 'Set Failed' in nodes; assert 'Check Dial Window' in nodes; assert any('scheduleTrigger' in n.get('type','') for n in w['nodes']); assert any('pick_next_lead' in json.dumps(n) for n in w['nodes']); assert any('initiate-call' in json.dumps(n) for n in w['nodes']); print(f'PASS: workflow has {len(w[\"nodes\"])} nodes with all required components')"</automated>
  </verify>
  <done>auto-dialer.json contains complete n8n workflow with Schedule Trigger (*/2 cron), dial window check (Luxon), active call guard, pick_next_lead RPC via HTTP Request, initiate-call POST with bearer auth, and error handling (409/429 -> queued, 500 -> failed)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify n8n workflow import and schema migration</name>
  <files>execution/n8n/auto-dialer.json, execution/backend/schema/006_retry_migration.sql</files>
  <action>
Human verifies that the auto-dialer workflow imports into n8n and the schema migration runs successfully. This covers the full Phase 6 output: schema changes (Plan 01) + workflow (Plan 02).

Steps:
1. Open execution/n8n/auto-dialer.json and confirm the node chain makes sense visually
2. Import the JSON into the n8n instance at n8n.srv1297445.hstgr.cloud (Settings -> Import from File)
3. After import: re-link the Supabase credential (placeholder SUPABASE_CRED_ID) to your actual Supabase service key credential
4. Verify that n8n env vars are set: SUPABASE_URL, SUPABASE_SERVICE_KEY, WEBHOOK_BASE_URL, DASHBOARD_SECRET_KEY
5. Run the schema migration 006_retry_migration.sql against Supabase (SQL Editor)
6. Click "Test Workflow" in n8n to trigger a single execution -- it should either stop at dial window check (if outside hours) or stop at empty queue (if no queued leads)
7. Do NOT activate the workflow yet -- that happens in Phase 9 (Wave 0 testing)
  </action>
  <verify>Human confirms workflow imports and test execution runs without errors</verify>
  <done>Workflow imported into n8n, schema migration applied, test execution completes without errors</done>
</task>

</tasks>

<verification>
1. Workflow JSON is valid and contains all required nodes (automated check)
2. Workflow can be imported into n8n without errors (manual verification)
3. Schema migration runs successfully in Supabase (manual verification)
4. Test execution in n8n runs through the expected path without crashes (manual verification)
</verification>

<success_criteria>
1. auto-dialer.json contains a complete n8n workflow with 12+ nodes
2. Schedule Trigger fires every 2 minutes
3. Dial window check uses Luxon for timezone-aware comparison
4. pick_next_lead() is called via HTTP Request (not Supabase node RPC)
5. Initiate call uses bearer auth with DASHBOARD_SECRET_KEY
6. Error branches correctly revert (409/429) or fail (500) leads
7. No DNC check exists in the workflow (backend handles it)
8. Workflow imports successfully into n8n and test execution completes
</success_criteria>

<output>
After completion, create `.planning/phases/06-auto-dialer-retry-logic/06-02-SUMMARY.md`
</output>
